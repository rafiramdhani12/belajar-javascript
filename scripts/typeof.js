/* 
! typeof
* typeof merupakan operator yg bisa kita gunakan untuk melihat tipe data sebuah value / variable
* karena js merupakan dynamic language , jadi kadang kita perlu mengecek tipe data, sebuah value / variable menggunakan operator typeof
* hasil dari operator typeof adalah string tipe datanya
*/

let data = 100;
const typeData = typeof data;

document.writeln(`<p>${typeData}</p>`);
